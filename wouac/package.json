{
  "name": "wouac",
  "version": "0.1.0",
  "description": "The woua language compiler — targets WAT/WASI, written in AssemblyScript",
  "scripts": {
    "build":       "asc assembly/index.ts --target release",
    "build:debug": "asc assembly/index.ts --target debug",
    "clean":       "rm -rf dist"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.0"
  },
  "dependencies": {
    "@assemblyscript/wasi-shim": "^0.1.0",
    "as-wasi": "^0.6.0"
  }
}
