;; @test defn
;; @feature define and call recursive functions
;; @expect 55

(include io)

(defn fib (n)
  (if (< n 2)
    n
    (+ (fib (- n 1)) (fib (- n 2)))))

(defn main ()
  (printf "%d\n" (fib 10)))
