;; @test while
;; @feature while loop with set! mutation counting from 1 to 5
;; @expect
;;   1
;;   2
;;   3
;;   4
;;   5

(include io)

(defn main ()
  (let i :i32 1
    (while (<= i 5)
      (printf "%d\n" i)
      (set! i (+ i 1)))))
