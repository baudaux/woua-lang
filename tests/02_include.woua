;; Test: include
;; (include io) brings in wasi_p1.woua → core.woua transitively.
;; Verifies that the include chain resolves library files correctly.
;; Expected output: include ok

(include io)

(defn main ()
  (print "include ok\n"))
