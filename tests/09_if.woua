;; @test if
;; @feature two-armed and three-armed if expressions
;; @expect
;;   positive
;;   done

(include io)

(defn sign (n)
  (if (> n 0)
    (print "positive\n")
    (print "non-positive\n")))

(defn main ()
  (sign 5)
  (if 1
    (print "done\n")))
